# INFORMASI PENJELASAN TENTANG FRAME SYNCHRONIZATION 
- Folder **exp 1.7 v1**: folder awal tanpa pengujian dan terintgrasi
- Folder **exp 1.7 v2**: folder untuk pengujian dengan sinyal AWGN
- Folder **exp 1.7 fixed point**: folder untuk melakukan cek ke fixed-point converter
- Folder yang memiliki **label "Sample-aware"**: Folder yang menggunakan arsitektur tanpa sampling batch dan frame sehingga sangat tidak efisien
- Folder **exp 1.7 v1_value-checking-flagging-precision**: Versi floating point dengan flagging dan precision yang sudah disesuaikan dengan Buku TA 232402005, sync point "##3#", pengecekan nilai minimum dan nilai maksimum dari hasil operasi per modul, dan kombinasi batch-frame 40-16
- Folder **exp 1.7 v1_with-value-checking**:  Versi floating point seperti kode sebelum dilakukan pengubahan (Flagging dan precision berbeda dengan buku TA dan sync point "#33#"), tetapi ditambahkan dengan pengecekan nilai minimum dan nilai maksimum dari hasil operasi per modul dan kombinasi batch-frame 40-16
- Folder **exp 1.7 fixed point v3**: Kode versi fixed-point dengan pengecekan konfigurasi fixed-point, nilai minimum, dan nilai maksimum per modul, menggunakan konfigurasi fixed-point: Q2.14 untuk sinyal _input_ dan _multiplier_, Q8.8 untuk _accumulator_, Q12.4 untuk _power calcultion_, dan Q15.1 untuk _batch summation_, dan kombinasi batch-frame 40-16
- Folder **exp 1.7 fixed point v3_awgn**: Kode versi fixed-point dengan pengecekan konfigurasi fixed-point, nilai minimum, dan nilai maksimum per modul, fixed-point: Q2.14 untuk sinyal _input_ dan _multiplier_, Q8.8 untuk _accumulator_, Q12.4 untuk _power calcultion_, dan Q15.1 untuk _batch summation_, kombinasi batch-frame 40-16, dan tambahan simulasi AWGN (20, 10, 5, 0, -5, -10, -20) dB
- Folder **exp 1.7 fixed point v4_awgn**: Kode versi fixed-point dengan pengecekan konfigurasi fixed-point, nilai minimum, dan nilai maksimum per modul, fixed-point: Q3.13 untuk sinyal _input_ dan _multiplier_, Q8.8 untuk _accumulator_, Q12.4 untuk _power calcultion_, dan Q15.1 untuk _batch summation_, kombinasi batch-frame 40-16, dan tambahan simulasi AWGN (20, 10, 5, 0, -5, -10, -20) dB
- Folder **exp 1.7 fixed point v4**: Kode versi fixed-point dengan pengecekan konfigurasi fixed-point, nilai minimum, dan nilai maksimum per modul, fixed-point: Q3.13 untuk sinyal _input_ dan _multiplier_, Q8.8 untuk _accumulator_, Q12.4 untuk _power calcultion_, dan Q15.1 untuk _batch summation_, dan kombinasi batch-frame 40-16
- Folder **exp 1.7 fixed point v5**: Kode seperti v4, tetapi ditambahkan sinyal *output* berupa *goertzel_enable* setelah simbol DTMF "3" terdeteksi
- Folder **exp 1.7 fixed point v6**: Kode seperti v5, tetapi top-level frame synchronization dijadikan sebagai fungsi *frame_syn* dengan *output* berupa *goertzel_enable* dan *input* berupa sinyal sinusoidal yang berisi *sync point* "##3#" dan *payload*
- Folder **exp 1.7 fixed point v7**: Kode seperti v6, tetapi menggunakan logika marking *Normalized Power Difference* pada total daya ($P_{697} \ge 0.55 \times (P_{697} + P_{941})$) yang diimplementasikan dengan perkalian silang (tanpa pembagian) untuk ketahanan terhadap redaman saluran dan derau asimetris
    - Memanfaatkan *guard floor* untuk memastikan ketidakadaan noise dalam mendeteksi simbol "#" atau "3" dan nilai batasannya adalah 32.0 sesuai dengan VHDL
    - Amplitudo puncak sinyal input disesuaikan menjadi 1.0 (skala 0.5) agar selaras dengan Q2.14 hardware